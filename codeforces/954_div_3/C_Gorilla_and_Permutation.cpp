#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define endl "\n"
void solve() {
    // given three number n , m , k
    // have to construct a permutation of lentgth n
    // sum of all the number of the permutation on a prefix of length i should not be greater than m and less then k
    
}
int32_t main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int tc = 1;
    cin >> tc;
    while (tc--) {
        solve();
    }
    return 0;
}
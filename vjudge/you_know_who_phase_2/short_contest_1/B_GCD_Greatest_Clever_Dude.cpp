#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define endl "\n"
void solve()
{

    string a, b;
    cin >> a >> b;

  
    if (a == b)
        cout << a << endl;
    else
        cout << 1 << endl;
}
int32_t main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int tc = 1;
    //in >> tc;
    while (tc--) {
        solve();
    }
    return 0;
}